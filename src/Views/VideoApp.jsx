import { Fragment } from "react";
import { NavBar, Modals } from "../Components";

const VideoApp = () => {
  return (
    <Fragment>
      <NavBar />
      <Modals />
    </Fragment>
  );
};

export default VideoApp;
